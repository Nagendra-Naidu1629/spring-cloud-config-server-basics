server:
  port: 8701

eureka:
  instance:
    hostname: caribbean.emea.fedex.com
  client:
    serviceUrl:
       defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/
    registerWithEureka: false
    fetchRegistry: false